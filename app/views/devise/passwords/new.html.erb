<section class="auth-page">
  <div class="auth-shell">
    <%= render "devise/shared/auth_navbar" %>

    <div class="auth-content">
      <section class="auth-card" aria-labelledby="auth-title">
        <p class="auth-kicker">RETROUVER L'ACCÈS</p>
        <h1 id="auth-title">Mot de passe oublié ?</h1>

        <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "auth-form" }) do |f| %>
          <%= f.error_notification %>

          <div class="auth-form-fields">
            <%= f.input :email,
                        label: "Adresse email",
                        required: true,
                        autofocus: true,
                        placeholder: "Entrez votre adresse email",
                        input_html: { autocomplete: "email" } %>
          </div>

          <div class="auth-actions">
            <%= f.button :submit, "Envoyer les instructions", class: "auth-submit" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </section>
    </div>
  </div>
</section>
